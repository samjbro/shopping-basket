<p id="notice"><%= notice %></p>


<h2>Your Neos Cart</h2>
<ul>
  <% @basket.line_items.each do |line_item| %>
    <li><%= line_item.quantity %> &times; <%= line_item.item.name %></li>
  <% end %>
</ul>
<!-- <%= link_to 'Edit', edit_basket_path(@basket) %> | -->
<%= link_to 'Continue shopping', shop_path %>
